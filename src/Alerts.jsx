import {
  AiFillCarryOut,
  AiFillCheckCircle,
  AiFillCheckSquare,
} from "react-icons/ai";

export default function Alerts({ color = "red", icon, title }) {
  const colorClasses = {
    blue: "bg-blue-500 hover:bg-blue-600 text-white",
    red: "bg-red-500 hover:bg-red-600 text-white",
    green: "bg-green-500 hover:bg-green-600 text-white",
  };

  return <>
  
  
  </>;
}
